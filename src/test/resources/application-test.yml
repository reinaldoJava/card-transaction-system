security:
  jwt:
    filter:
      enabled: false

jwt:
  secret: test-secret-key-for-unit-tests-only-32b
  expiration-hours: 24
logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure: ERROR
management:
  otlp:
    metrics:
      export:
        enabled: false